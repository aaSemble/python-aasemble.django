dependencies:
  override:
    - bash -c 'rm -rf $VIRTUAL_ENV; virtualenv $VIRTUAL_ENV'
    - pip install -U pip
    - pip install -U "Django<1.9"
    - pip install -r requirements.txt
    - pip install -r test-requirements.txt
test:
  override:
    - python manage.py test
